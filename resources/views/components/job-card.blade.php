<div class="col-lg-4">
    <div class="job-block">
        <div class="inner-box">
            <!-- <h4><a href="{{ url('/jobs/' . $job->id) }}">{{ $job->title }}</a></h4>
            <p><span class="icon flaticon-briefcase"></span> {{ $job->company }}</p>
            <p><span class="icon flaticon-map-locator"></span> {{ $job->location }}</p>
            <a href="{{ url('/jobs/' . $job->id) }}" class="apply-btn">Apply Now</a> -->
        </div>
    </div>
</div>
